# Alfa-Ecosystem-Task

### created by bun create vite@latest

Stack: React, Typescript, Zustand, Shadcn with Tailwind (UI), React hook form (Forms), React router (routing), Lucide React (icons), Zod && @hook/resolvers (Validation), Tanstack React Query && Axios (for caching and requests)
